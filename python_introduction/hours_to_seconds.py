# This program converts hours to seconds

# Define variables
hours = 2

# Change the hours to seconds (1 hour = 3600 seconds)
seconds = hours * 3600

# Print the result to the console
print(f"{hours} hour(s) is {seconds} seconds")